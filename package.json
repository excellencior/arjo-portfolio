{
  "name": "arjo-portfolio-root",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "build:client": "cd client && npm run build",
    "start:server": "cd server && npm start",
    "dev:all": "concurrently \"cd client && npm run dev\" \"cd server && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
